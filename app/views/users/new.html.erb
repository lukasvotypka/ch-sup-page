<h1>New user</h1>

<% form_for(@user) do |f| %>

  <%= render :partial=>"form", :locals => { :user => @user}, :object => f  %>
  
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', users_path %>